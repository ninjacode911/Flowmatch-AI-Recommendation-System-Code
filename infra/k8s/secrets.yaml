# Secret values are base64-encoded placeholders.
# Replace with real secrets before deploying. Never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: recsys-secrets
  namespace: recsys
  labels:
    app.kubernetes.io/part-of: flowmatch-recsys
type: Opaque
data:
  POSTGRES_USER: cmVjc3lz              # recsys
  POSTGRES_PASSWORD: CHANGEME          # replace with real base64-encoded password
  ANTHROPIC_API_KEY: CHANGEME          # replace with real base64-encoded key
